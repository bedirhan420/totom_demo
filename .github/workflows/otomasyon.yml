name: SauceDemo Otomasyon Hattı 🚀

on:
  push:
    branches: [ main ] # Kodu main'e attığında çalışır
  workflow_dispatch:   # Elle tetiklemek için

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test-ve-rapor:
    runs-on: ubuntu-latest
    steps:
      - name: Depoyu Çek (Checkout)
        uses: actions/checkout@v4

      - name: Python Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Kütüphaneleri Yükle
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Playwright Tarayıcılarını Kur
        run: playwright install chromium

      - name: Testleri Koştur (SauceDemo)
        # --alluredir: Sonuçları 'allure-results' klasörüne yazar
        # continue-on-error: Test başarısız olsa bile durma, raporu üret.
        run: pytest playwright_test/test_sauce_demo.py --trace on  --alluredir=allure-results --video=retain-on-failure
        continue-on-error: true
      
      - name: Trace Dosyalarını Yükle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: test-results

      - name: Allure Raporunu İnşa Et 📊
        uses: simple-elf/allure-report-action@master
        if: always() # Testler patlasa bile bu adım çalışsın
        with:
          allure_results: allure-results
          allure_report: allure-report
          gh_pages: gh-pages

      - name: Raporu GitHub Pages için Hazırla
        uses: actions/upload-pages-artifact@v3
        with:
          path: allure-report

  deploy:
    needs: test-ve-rapor
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Canlıya Al (Deploy) 🌍
        id: deployment
        uses: actions/deploy-pages@v4
